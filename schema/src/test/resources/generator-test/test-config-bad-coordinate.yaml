imports:
  - "car-lib"

schemas:
  - schemas/record.avsc

generators:
  - path: templates
    templates:
      namespace: "{{schema.namespace}}"
      name: "{{schema.name}}"
      fullname: "{{namespace}}.{{name}}"
      folder: "generated"
      filename: "{{name}}.json"
